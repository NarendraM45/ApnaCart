package com.apnacart.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Brand (Amazon Style) ──────────────────────────
val AmazonNavy       = Color(0xFF131921)   // Primary Header / Navigation
val AmazonNavyLight  = Color(0xFF232F3E)   // Sub-header
val AmazonOrange     = Color(0xFFFF9900)   // Primary CTA, highlights
val AmazonYellow     = Color(0xFFFFD814)   // Secondary CTA (Add to cart)
val AmazonCyan       = Color(0xFF007185)   // Links, highlights

// ── Neutrals ────────────────────────────────────────
val OffWhite         = Color(0xFFF8F9FA)   // Light background
val LightGray        = Color(0xFFEAEDED)   // Background of Amazon
val MidGray          = Color(0xFFDDDDDD)   // Borders, separators
val DarkText         = Color(0xFF0F1111)   // Primary text
val SubText          = Color(0xFF565959)   // Secondary text

// ── Semantic ────────────────────────────────────────
val ErrorRed         = Color(0xFFB12704)
val SuccessGreen     = Color(0xFF007600)   // In Stock
val WarningAmber     = Color(0xFFFFA000)
val StarGold         = Color(0xFFDE7921)   // Rating stars
val DiscountRed      = Color(0xFFCC0C39)   // Discount badges

// ── Dark Theme (Minimal Dark Navy) ─────────────────
val BackgroundDark   = Color(0xFF0F0F17)
val SurfaceDark      = Color(0xFF131921)
val CardDark         = Color(0xFF1A212A)
val OutlineDark      = Color(0xFF2E3846)
